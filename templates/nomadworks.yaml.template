# NomadWorks Configuration
enabled: true
status: initialized # initialized | onboarded

# Global Defaults: Applied to all agents unless overridden
defaults:
  provider: null # e.g., google, openai, anthropic
  model: null    # e.g., gemini-3-flash-preview, gpt-4o
  # temperature: 0.2
  # permissions:
  #   read: "allow"
  #   write: "allow"
  #   bash: "allow"

# Agent Specific Overrides
agents:
  # Example override:
  # developer:
  #   model: "openai/gpt-4o"
  #   temperature: 0.1
  #   reasoningEffort: "high"
  #   textVerbosity: "low"
  #   tools_add: ["custom_tool"]
  #   tools_remove: ["playwright"]
